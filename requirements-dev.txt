# Development / CI tooling only.
#
# Runtime needs just a WeeWX 5.4+ install and an `ffmpeg` binary on PATH;
# the extension itself imports nothing outside the Python standard library
# and WeeWX.
ruff>=0.16.5
pytest>=8
